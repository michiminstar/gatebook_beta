<%= form_for(current_user.user_friendships.find_by(friend_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "友達から削除", class: "btn delete-btn" %>
<% end %>
